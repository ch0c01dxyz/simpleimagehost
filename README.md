# Simple Image Host

This is an tet another simple image hosting that allows you to upload images and have it embedded in Discord messages.

## Features

- Upload images (manual / api)
- Embed images in Discord messages
- Customize embeds (settings in `settings.ts`)
- Download configuration files for ShareX